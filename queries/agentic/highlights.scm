; The chat buffer parses as the private `agentic` language (a clone of the
; markdown parser) so its folds query is isolated from real markdown buffers.
; Highlighting is identical to markdown — including the conceal_lines metadata
; on fence delimiters that the trackpad-scroll workaround depends on.
; inherits markdown
