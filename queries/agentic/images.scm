; inherits markdown

; Registers `agentic` as a snacks.image language (Snacks.image.langs() globs
; queries/*/images.scm) so AgenticChat buffers attach. Math is found in the
; injected `latex` tree via snacks' own queries/latex/images.scm.
